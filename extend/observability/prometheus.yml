global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/prometheus-alerts.yml

scrape_configs:
  - job_name: "moyuan-backend"
    metrics_path: /api/metrics
    static_configs:
      - targets:
          - backend:38000
